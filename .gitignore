node_modules/
dist/
coverage/

# Env files hold secrets. Only the example is committed.
.env
.env.*
!.env.example

# Logs & OS cruft
*.log
.DS_Store

# TLS material (Postgres provider CA bundles, e.g. RDS)
*.pem
*.crt
certs/
